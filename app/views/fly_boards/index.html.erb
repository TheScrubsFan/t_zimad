<h1>
  Fly Board
</h1>

<table>
  <tr>
    <th>N</th>
    <th>State</th>
    <th>Action</th>
  </tr>

  <% @planes.each do |plane| %>
    <tr>
      <td>
        <%= plane['number'] %>
      </td>
      <td>
        <%= plane['state'] %>
      </td>
      <td>
        <%= link_to 'On take-off',
                    plane_path(plane['number'], plane: { state: 'takeoff' }),
                    method: :patch if plane['state'] == 'hangar' %>
      </td>
    </tr>
  <% end %>
</table>
